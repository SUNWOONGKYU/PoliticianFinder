# V40 추가 평가 가이드

**목적**: 평가 누락 시 추가 평가를 수행하는 방법
**대상**: 모든 4개 AI (Claude, ChatGPT, Gemini, Grok)
**최종 업데이트**: 2026-02-12

---

## 📋 언제 추가 평가가 필요한가?

### 상황 1: 평가 상태 확인 시 누락 발견
```bash
python check_evaluation_status.py --politician "박주민"

# 결과 예시:
expertise (179개):
  Claude: 179/179 ✅
  ChatGPT: 175/179 ⚠️  # 4개 누락!
  Gemini: 179/179 ✅
  Grok: 179/179 ✅
```

### 상황 2: 오류로 인한 평가 중단
- 네트워크 오류
- API 한도 초과
- CLI 타임아웃
- 기타 예외 발생

### 상황 3: 새로운 데이터 수집 후
- 검증 스크립트 실행 후 데이터 추가
- 재수집 후 평가 필요

---

## ✅ 추가 평가 방법 (AI별)

**⭐ 핵심 원칙**: 각 AI의 Helper 스크립트를 다시 실행하면 자동으로 미평가 데이터만 찾아서 평가합니다!

---

### 1. Claude 추가 평가

**스크립트**: `scripts/helpers/claude_eval_helper.py`
**방식**: Anthropic API

#### 단일 카테고리 추가 평가
```bash
cd V40/scripts/helpers

python claude_eval_helper.py \
  --politician_id=8c5dcc89 \
  --politician_name="박주민" \
  --category=expertise \
  --batch_size=25
```

#### 전체 카테고리 추가 평가 (10개)
```bash
cd V40/scripts/helpers

for cat in expertise leadership vision integrity ethics accountability transparency communication responsiveness publicinterest; do
  echo "=== Claude $cat ==="
  python claude_eval_helper.py \
    --politician_id=8c5dcc89 \
    --politician_name="박주민" \
    --category=$cat \
    --batch_size=25
done
```

**특징**:
- ✅ 미평가 데이터 자동 감지
- ✅ 배치 크기: 25개 (기본값)
- ⚠️ API 키 필요: `ANTHROPIC_API_KEY`

---

### 2. ChatGPT 추가 평가

**스크립트**: `scripts/helpers/codex_eval_helper.py`
**방식**: Codex CLI (stdin)

#### 단일 카테고리 추가 평가
```bash
cd V40/scripts/helpers

python codex_eval_helper.py \
  --politician_id=8c5dcc89 \
  --politician_name="박주민" \
  --category=expertise \
  --batch_size=25
```

#### 전체 카테고리 추가 평가 (10개)
```bash
cd V40/scripts/helpers

for cat in expertise leadership vision integrity ethics accountability transparency communication responsiveness publicinterest; do
  echo "=== ChatGPT $cat ==="
  python codex_eval_helper.py \
    --politician_id=8c5dcc89 \
    --politician_name="박주민" \
    --category=$cat \
    --batch_size=25
done
```

**특징**:
- ✅ 미평가 데이터 자동 감지
- ✅ 배치 크기: 25개 (기본값)
- ✅ Codex CLI 사용 (stdin 방식)
- ⚠️ Codex CLI 설치 필요

---

### 3. Gemini 추가 평가

**스크립트**: `scripts/workflow/evaluate_gemini_subprocess.py`
**방식**: Gemini CLI Subprocess

#### 단일 카테고리 추가 평가
```bash
cd V40/scripts/workflow

python evaluate_gemini_subprocess.py \
  --politician "박주민" \
  --category "expertise"
```

#### 전체 카테고리 추가 평가 (10개)
```bash
cd V40/scripts/workflow

for cat in expertise leadership vision integrity ethics accountability transparency communication responsiveness publicinterest; do
  echo "=== Gemini $cat ==="
  python evaluate_gemini_subprocess.py \
    --politician "박주민" \
    --category "$cat"
done
```

**특징**:
- ✅ 미평가 데이터 자동 감지
- ✅ 배치 크기: 25개 (자동)
- ✅ Gemini CLI 사용 (Subprocess)
- ⚠️ Google 계정 인증 필요 (`gemini auth`)

---

### 4. Grok 추가 평가

**스크립트**: `scripts/helpers/grok_eval_helper.py`
**방식**: xAI API

#### 단일 카테고리 추가 평가
```bash
cd V40/scripts/helpers

python grok_eval_helper.py \
  --politician_id=8c5dcc89 \
  --politician_name="박주민" \
  --category=expertise \
  --batch_size=25
```

#### 전체 카테고리 추가 평가 (10개)
```bash
cd V40/scripts/helpers

for cat in expertise leadership vision integrity ethics accountability transparency communication responsiveness publicinterest; do
  echo "=== Grok $cat ==="
  python grok_eval_helper.py \
    --politician_id=8c5dcc89 \
    --politician_name="박주민" \
    --category=$cat \
    --batch_size=25
done
```

**특징**:
- ✅ 미평가 데이터 자동 감지
- ✅ 배치 크기: 25개 (기본값)
- ✅ xAI API 사용 (grok-3 모델)
- ⚠️ API 키 필요: `XAI_API_KEY`

---

## 📊 추가 평가 워크플로우

### Step 1: 평가 상태 확인
```bash
cd V40/scripts/utils
python check_evaluation_status.py --politician "박주민"
```

**출력 예시**:
```
expertise (179개):
  Claude: 179/179 ✅
  ChatGPT: 175/179 ⚠️  (4개 누락)
  Gemini: 179/179 ✅
  Grok: 179/179 ✅
```

### Step 2: 누락된 AI 확인
- 위 예시에서 **ChatGPT**가 **expertise** 카테고리에서 4개 누락

### Step 3: 해당 AI Helper 실행
```bash
cd V40/scripts/helpers
python codex_eval_helper.py \
  --politician_id=8c5dcc89 \
  --politician_name="박주민" \
  --category=expertise \
  --batch_size=25
```

### Step 4: 결과 확인
```bash
cd V40/scripts/utils
python check_evaluation_status.py --politician "박주민"
```

**기대 결과**:
```
expertise (179개):
  Claude: 179/179 ✅
  ChatGPT: 179/179 ✅  (추가 평가 완료!)
  Gemini: 179/179 ✅
  Grok: 179/179 ✅
```

---

## ⚠️ 주의사항

### 1. 중복 평가 방지
모든 Helper 스크립트는 **자동으로 중복 방지** 기능이 내장되어 있습니다:
```python
def get_unevaluated_data(politician_id, category):
    # 1. 전체 수집 데이터
    # 2. 이미 평가된 데이터
    # 3. 차집합 (미평가만 추출)
```

→ **같은 스크립트를 여러 번 실행해도 안전합니다!**

### 2. API 한도 주의
- **Claude**: Anthropic API 한도 확인
- **ChatGPT**: Codex CLI는 한도 없음 (로컬 실행)
- **Gemini**: Gemini CLI는 한도 없음 (로컬 실행)
- **Grok**: xAI API 한도 확인

### 3. 배치 크기
- 기본값: **25개**
- Gemini는 자동 조정 (50개까지 가능)
- 변경 시: `--batch_size` 옵션 사용

### 4. 타임아웃
- Claude, ChatGPT, Grok: 60초 기본 타임아웃
- Gemini: 600초 (10분) 타임아웃

---

## 🚫 더 이상 사용하지 않는 방법

### ❌ Deprecated: `evaluate_missing_v40_api.py`

**이유**:
1. ChatGPT, Grok만 지원 (Claude, Gemini 미지원)
2. API 방식만 지원 (실제 CLI 방식과 불일치)
3. 각 Helper가 이미 미평가 조회 기능 내장

**대체 방법**: 각 AI별 Helper 스크립트 직접 실행 (위 가이드 참조)

---

## 📝 실전 예시

### 예시 1: 단일 AI, 단일 카테고리
```bash
# ChatGPT의 expertise 추가 평가
cd V40/scripts/helpers
python codex_eval_helper.py \
  --politician_id=8c5dcc89 \
  --politician_name="박주민" \
  --category=expertise \
  --batch_size=25
```

### 예시 2: 단일 AI, 전체 카테고리
```bash
# Gemini 전체 카테고리 추가 평가
cd V40/scripts/workflow
for cat in expertise leadership vision integrity ethics accountability transparency communication responsiveness publicinterest; do
  python evaluate_gemini_subprocess.py --politician "박주민" --category "$cat"
done
```

### 예시 3: 모든 AI, 단일 카테고리
```bash
# 모든 AI의 publicinterest 추가 평가

# Claude
cd V40/scripts/helpers
python claude_eval_helper.py --politician_id=8c5dcc89 --politician_name="박주민" --category=publicinterest --batch_size=25

# ChatGPT
python codex_eval_helper.py --politician_id=8c5dcc89 --politician_name="박주민" --category=publicinterest --batch_size=25

# Gemini
cd ../workflow
python evaluate_gemini_subprocess.py --politician "박주민" --category "publicinterest"

# Grok
cd ../helpers
python grok_eval_helper.py --politician_id=8c5dcc89 --politician_name="박주민" --category=publicinterest --batch_size=25
```

---

## 🎯 요약

**핵심 메시지**:
> 추가 평가는 각 AI의 Helper 스크립트를 다시 실행하면 됩니다!

**4개 AI Helper**:
1. ✅ `claude_eval_helper.py` (Anthropic API)
2. ✅ `codex_eval_helper.py` (Codex CLI)
3. ✅ `evaluate_gemini_subprocess.py` (Gemini CLI)
4. ✅ `grok_eval_helper.py` (xAI API)

**자동 기능**:
- ✅ 미평가 데이터 자동 감지
- ✅ 중복 방지
- ✅ 배치 처리 (25개씩)

---

**최종 업데이트**: 2026-02-12
**관련 문서**: `추가평가_방법_연구보고서.md`
